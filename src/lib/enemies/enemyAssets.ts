/** Default arena preview enemy — centre of the floor plate. */
export const CENTER_ENEMY_MODEL_URL =
  "/models/enemies/px27-android-character.glb";

export const CENTER_ENEMY_POSITION = { x: 0, y: 0, z: 0 } as const;
/** Full mesh height; player eye/camera height is 1.8m, so the enemy needs headroom above that. */
export const CENTER_ENEMY_HEIGHT_METERS = 2.1;
